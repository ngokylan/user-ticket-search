export type PlaceholderHeaderProps = {
  /** id attribute, optional id */
  id?: string;
  /** If it has Header action buttons */
  hasButtons: boolean;
  /** If it has the back button */
  hasBackButton: boolean;
  /** If it has sub heading */
  hasSubHeading: boolean;
  /** If it has tabs */
  hasTabs: boolean;
  /** Number of tabs */
  numberOfTabs?: number;
};
